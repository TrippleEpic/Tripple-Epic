import React from "react";
import PlayersContainer from "./PlayersContainer";

const Players = () => {
  return (
    <div className="relative bg-[#29325E] w-full max-h-[500px] flex">
      <PlayersContainer />
    </div>
  );
};

export default Players;
